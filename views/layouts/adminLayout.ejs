<!doctype html>
<html lang="es">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title><%= title || 'Administrador' %> | Biblioteca Digital</title>
  <!-- CSS Único -->
  <link rel="stylesheet" href="/css/main.css">
  <!-- Estilos de Admin -->
  <link rel="stylesheet" href="/css/admin.css">
  <%- style %>
</head>
<body class="admin-shell <%= typeof pageClass !== 'undefined' ? pageClass : '' %>">
  <%- include('../partials/topbar') %>
  <main class="admin-main page <%= typeof pageClass !== 'undefined' ? pageClass : '' %>">
    <%- include('../partials/alertas') %>
    <%- body %>
  </main>
  <%- script %>
</body>
</html>
